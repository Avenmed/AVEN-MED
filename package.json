{
  "name": "aven-med",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sitemap": "node --import jiti/register scripts/generate-sitemap.mjs",
    "prerender": "node --import jiti/register scripts/prerender.mjs",
    "build": "node --import jiti/register scripts/generate-sitemap.mjs && vite build && node --import jiti/register scripts/prerender.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "jiti": "^2.4.2",
    "vite": "^5.4.11"
  }
}
